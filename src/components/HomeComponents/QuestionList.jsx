export default function QuestionList(catId) {
  // iterate over the array and create a list item for each item
  // const listItems = data.data?.map((question, index) => (
  //   <div
  //     key={index}
  //     style={{ marginBottom: 2 }}>
  //     {question.content}
  //   </div>
  // ))

  return (
    <div
      style={{
        height: 150,
        backgroundColor: 'white',
        borderRadius: 10,
        marginTop: 4,
        padding: 10,
      }}>
      {/* {listItems} */}
    </div>
  )
}
